# Convert `writing-mode` `horizontal-tb` value to `initial`

The `initial` value for `writing-mode` is `horizontal-tb`, so they are equivalent.
However, `initial` is both shorter and a global value (giving more opportunities
for gzipping).

* https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/writing-mode#formal_definition
