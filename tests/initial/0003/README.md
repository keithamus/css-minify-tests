# Convert `border-collapse` `separate` value to `initial`

The `initial` value for `border-collapse` is `separate`, so they are equivalent.
However, `initial` is both shorter and a global value (giving more opportunities
for gzipping).

* https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-collapse#formal_definition
