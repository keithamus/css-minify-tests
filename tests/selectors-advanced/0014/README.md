## Sorting `:is()` selectors

Selector minification in `:is` can be complex. To keep expectations consistent
the order should be alphabetized. This also increases the liklihood of text
repetition, giving better compression in gzip/brotli.
