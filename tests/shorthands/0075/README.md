# Combine background position X and Y

If `background-position-x` and `background-position-y` are both used in the same
rule, and there are no other background properties to be combined with, they can
be combined into a single `background-position`.
