# Border-color shorthand with color keyword & hex touching

The top/bottom and left/right match in this example, so they can be combined
when converting to the shorthand for border color. The `blue` value can be
converted to hex, and there is no need to put a space after `red`, since the
color keyword and hex values can be parsed as separate tokens, even when
touching.
