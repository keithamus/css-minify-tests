# Omit default `syntax: "*"`

The `syntax` descriptor defaults to `"*"` (universal syntax), so explicit `syntax: "*"` can be safely omitted. Non-default descriptors like `inherits: false` and `initial-value` are preserved when meaningful.
