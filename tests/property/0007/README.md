# Remove whitespace around `syntax` values in `@property`

An `@property` rule can have multiple values set for the `syntax` descriptor,
separated by a pipe symbol (`|`). Whitespace adjacent to the pipe can be
removed.
