# Replace color-start with `0` when it matches the prior color-stop

You can group the start and stop of a color in a gradient together with a space
separation. You can also use a unitless `0` to indicate the first position of a
color is the same as the stop of the previous color.
