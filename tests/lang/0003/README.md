# Lowercase language-codes in `:lang()`

Language-codes used by `:lang()` are case-insensitive. Lowercasing them
consistently, much like with hex values in color, can potentially aid in gzip
compression.
