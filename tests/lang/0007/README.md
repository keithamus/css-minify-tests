# Combine rules that only differ in language-code in `:lang()`

If two rules are identical except for the language-code used in `:lang()`, they
can be combined, with the language-codes comma-separated inside the `:lang()`
function.
