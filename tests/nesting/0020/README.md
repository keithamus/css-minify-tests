# Introduce nesting to shorten repeated class selectors

This test validates simple repeated selectors with matching parent classes and
different child classes get nested during minificaiton to reduce the duplication
of the parent class in the selector.
