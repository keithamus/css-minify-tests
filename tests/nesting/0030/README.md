# Merge nested and un-nested rules that are otherwise identical

If a selector has a descendant with a rule declaration that is identical to
a rule of the same descendant but nested, merge the two if adjacent.
