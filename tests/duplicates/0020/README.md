# Remove overridden properties in rules with multiple selectors

If the properties of all selectors in a rule get overridden later in the
stylesheet, then that property can be removed from the earlier rule. If no
properties are left in the rule (it is empty), the entire rule may be discarded.
