# Remove space after parens before "and" in media query

If a media query contains an `and` following a condition wrapped in
parenthesis, there is no need to include a space to separate them. It does
however require a space after the `and`.
