# Escape semi-colons in attribute selectors

When an attribute selector includes a semi-colon it must either be surrounded in
quotes or escaped. If the total number of escapes required is 3 or more, then
prefer quotes to avoid additional characters.
