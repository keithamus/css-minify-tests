# Convert `box-sizing` `content-box` value to `initial`

The `initial` value for `box-sizing` is `content-box`, so they are equivalent.
However `initial` is both shorter, and a global value, giving more opportunities
for gzipping.
