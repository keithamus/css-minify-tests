# Merge selectors with comma and perform advanced property shorthand

Remove the comment, merge rules with identical selectors, apply font shorthand.

Combines:

* `comments/0001` - Basic comment removal
* `mergin/0019` - Merge identical selector separated by non-overlapping rules
* `shorthands/0017` - Font longhand merge
